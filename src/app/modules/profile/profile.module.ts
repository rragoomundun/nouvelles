import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { ProfileRoutes } from './profile.routes';

@NgModule({
  declarations: [],
  imports: [CommonModule, ProfileRoutes],
})
export class ProfileModule {}
